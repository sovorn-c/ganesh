story_id: e03s01
spec: e03s01-classification-and-policy-records.md
title: Classification and Destination/Purpose Policy Records
status: passing
bcps: 7
tasks:
  - id: e03s01-t01
    description: Add validated classification, permission, restriction, and policy-decision types plus the additive SQLite schema migration.
    verify: "npm run build && node --test --test-name-pattern='e03s01.*(schema|classification|policy)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - policy
        - schema
        - Security Review
  - id: e03s01-t02
    description: Persist exact-version classifications, scoped destination/purpose permissions, validity, and append-only status history.
    verify: "npm run build && node --test --test-name-pattern='e03s01.*(record|grant|expiry|withdraw)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - policy
        - persistence
        - Security Review
  - id: e03s01-t03
    description: Implement deterministic fail-closed policy evaluation with attributable allow and denial results.
    verify: "npm run build && node --test --test-name-pattern='e03s01.*(evaluate|deny|unclassified|scope)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - authorization
        - Security Review
  - id: e03s01-t04
    description: Prove local-only defaults, forged approval rejection, policy history, and compatibility with the E02 stores.
    verify: "npm run build && node --test --test-name-pattern='e03s01.*(local|forged|history|compat)' dist/test/*.test.js"
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - integration
        - provenance
