story_id: e15s03
spec: e15s03-crash-disk-full-corruption-and-concurrent-launches.md
title: Crash, Disk-Full, Corruption and Concurrent-Launch Hardening
status: passing
bcps: 7
tasks:
  - id: e15s03-t01
    description: Inject crash boundaries around artifact and decision writes and prove reopen exposes only complete states.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node scripts/require-test-match.mjs 'e15s03.*(crash|half|complete|AC-17)' dist/tests/*/*.test.js"
    risk: P0
    security: low
    scenarios: [SC-e15s03-P0-01]
    status: passing
    allure:
      severity: critical
      categories: [recovery, durability, AC-17]
  - id: e15s03-t02
    description: Add failAt disk-full ENOSPC abort that leaves no new current reference and records a failed checkpoint.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node scripts/require-test-match.mjs 'e15s03.*(disk-full|ENOSPC)' dist/tests/*/*.test.js"
    risk: P0
    security: low
    scenarios: [SC-e15s03-P0-02]
    status: passing
    allure:
      severity: critical
      categories: [recovery, storage]
  - id: e15s03-t03
    description: Add pid-reentrant write lock with foreign-pid deny, dead-pid steal and same-process reentry, and prove no new security findings in affected lock paths.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node scripts/require-test-match.mjs 'e15s03.*(lock|pid|concurrent|reentry)' dist/tests/*/*.test.js"
    risk: P0
    security: medium
    scenarios: [SC-e15s03-P0-03]
    status: passing
    allure:
      severity: critical
      categories: [concurrency, Security Review]
  - id: e15s03-t04
    description: Report corrupt artifacts, keep pending lifecycle operations pending, and keep released E01-E08/E14 behavior passing.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run typecheck && npm run lint && npm run build && npm test && node scripts/require-test-match.mjs 'e15s03.*(corrupt|pending|lifecycle|regression)' dist/tests/*/*.test.js"
    risk: P1
    security: medium
    scenarios: [SC-e15s03-P1-04]
    status: passing
    allure:
      severity: high
      categories: [recovery, lifecycle, regression]
