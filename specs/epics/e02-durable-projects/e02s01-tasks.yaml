story_id: e02s01
spec: e02s01-create-and-reopen-durable-projects.md
title: Create and Reopen Durable Projects and Immutable Artifact Versions
status: passing
bcps: 8
tasks:
  - id: e02s01-t01
    description: Bind the Node.js 24 SQLite API and implement the versioned project schema, validated project creation, and reopen path (SC-e02s01-P0-01).
    verify: "npm run build && node --test --test-name-pattern='e02s01.*project|project.*schema|sqlite.*runtime' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - persistence
        - schema
  - id: e02s01-t02
    description: Implement content-addressed artifact finalization, hash validation, immutable version records, and path-bound storage (SC-e02s01-P0-01).
    verify: "npm run build && node --test --test-name-pattern='e02s01.*artifact|artifact.*immutable|hash' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - integrity
        - artifacts
  - id: e02s01-t03
    description: Persist exact version-to-version dependency edges and explicit available, missing, corrupt, and unavailable inspection outcomes (SC-e02s01-P0-02, SC-e02s01-P1-03).
    verify: "npm run build && node --test --test-name-pattern='e02s01.*depend|missing.*artifact|corrupt.*artifact' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - provenance
        - failure-modes
  - id: e02s01-t04
    description: Add close/reopen, offline, and failed-registration fixtures that prove no incomplete current reference becomes visible (SC-e02s01-P0-02, SC-e02s01-P1-03).
    verify: "npm run build && node --test --test-name-pattern='e02s01.*reopen|offline.*project|atomic.*registration' dist/test/*.test.js"
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - recovery
        - offline
