story_id: e04s02
spec: e04s02-commitment-lifecycle-and-current-readiness.md
title: Commitment Lifecycle and Current Readiness
status: passing
bcps: 5
tasks:
  - id: e04s02-t01
    description: Add commitment lifecycle and readiness types plus additive history tables with schema migration compatibility.
    verify: "npm run build && node --test --test-name-pattern='e04s02.*(schema|lifecycle|readiness)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - lifecycle
        - persistence
        - Security Review
  - id: e04s02-t02
    description: Implement revision, reopen, archive, and supersession transitions with immutable linkage and command idempotency.
    verify: "npm run build && node --test --test-name-pattern='e04s02.*(revis|reopen|archiv|supersed)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - history
        - state-transitions
        - integration
  - id: e04s02-t03
    description: Assess current readiness from exact dependencies, availability, impact notices, and E03 policy status without changing historical commitments.
    verify: "npm run build && node --test --test-name-pattern='e04s02.*(ready|stale|blocked|dependency|policy|history)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - readiness
        - dependency-impact
        - Security Review
  - id: e04s02-t04
    description: Add restart, concurrent-action, inspection, and E01–E03 regression fixtures.
    verify: "npm run build && npm test && node --test --test-name-pattern='e04s02.*(restart|concurr|inspect|regression)' dist/test/*.test.js"
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - recovery
        - concurrency
        - regression
