story_id: e01s01
spec: e01s01-reproducible-typescript-runtime-and-locked-command-baseline.md
title: Reproducible TypeScript Runtime and Locked Command Baseline
status: passing
bcps: 6
tasks:
  - id: e01s01-t01
    description: Create the Node.js 24/npm manifest, package-manager metadata, selected local Pi dependency binding, lockfile, and direct dependency/license inventory (SC-e01s01-P0-02).
    verify: >-
      node -e "const p=require('./package.json'); if(p.engines?.node !== '>=24.0.0 <25') process.exit(1); if(!p.packageManager || !p.dependencies || !p.devDependencies) process.exit(1); require('fs').accessSync('package-lock.json'); console.log('runtime manifest ok')"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - runtime
        - reproducibility
  - id: e01s01-t02
    description: Add strict TypeScript configuration, a minimal public baseline entry point, and a built-in test that proves the compiled entry point works (SC-e01s01-P1-03).
    verify: "npm run build && node --test --test-name-pattern='baseline|entry' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - build
        - tests
  - id: e01s01-t03
    description: Wire foreground dev, test, build, lint, typecheck, and preflight scripts to project-local dependencies with non-zero failure behavior (SC-e01s01-P0-01).
    verify: "npm run typecheck && npm run lint && npm run build && npm test"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - command-contract
        - quality-gates
  - id: e01s01-t04
    description: Prove the locked clean-install path and complete command contract without relying on global Pi configuration (SC-e01s01-P0-01).
    verify: "npm ci --ignore-scripts && npm run preflight"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - clean-install
        - local-first
