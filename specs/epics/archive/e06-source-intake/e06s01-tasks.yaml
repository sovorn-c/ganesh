story_id: e06s01
spec: e06s01-immutable-local-text-and-markdown-intake.md
title: Immutable Local Text and Markdown Intake
status: passing
bcps: 6
tasks:
  - id: e06s01-t01
    description: Add source-version, import-operation, extraction, diagnostic and locator types plus exact v1 additive schema initialization paths, atomic completion and retry/recovery coverage, reusing artifact logical IDs and versions as source identity.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e06s01.*(schema|source record|reopen|migration|atomic|retry|recovery|orphan)' dist/tests/*/*.test.js"
    risk: P0
    scenarios: [SC-e06s01-P0-01, SC-e06s01-P0-02]
    status: passing
    allure:
      severity: critical
      categories: [sources, persistence, provenance]
  - id: e06s01-t02
    description: Harden capability-scoped file reads against sibling-prefix and symlink escapes, wrong-project use, missing operations, non-regular files, byte limits and source mutation during read.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e06s01.*(capability|path|symlink|project|limit|mutation)' dist/tests/*/*.test.js"
    risk: P0
    scenarios: [SC-e06s01-P0-03]
    status: passing
    allure:
      severity: critical
      categories: [authority, project-isolation, Security Review]
  - id: e06s01-t03
    description: Import UTF-8 text and Markdown as immutable acquired source versions with byte-identical originals, stable line/heading locators, access level and bounded extraction diagnostics.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e06s01.*(text|markdown|original|locator|diagnostic)' dist/tests/*/*.test.js"
    risk: P0
    scenarios: [SC-e06s01-P0-01, SC-e06s01-P0-02, SC-e06s01-P1-04]
    status: passing
    allure:
      severity: critical
      categories: [source-intake, locators, integrity]
  - id: e06s01-t04
    description: Prove unclassified imports remain local-only, failures create no successful source record, diagnostics redact content, and all released E01-E04 behavior remains passing.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run typecheck && npm run lint && npm run build && npm test && node --test --test-name-pattern='e06s01.*(local.only|failure|redact|regression)' dist/tests/*/*.test.js"
    risk: P1
    scenarios: [SC-e06s01-P0-01, SC-e06s01-P0-03, SC-e06s01-P1-04]
    status: passing
    allure:
      severity: high
      categories: [privacy, recovery, regression]
