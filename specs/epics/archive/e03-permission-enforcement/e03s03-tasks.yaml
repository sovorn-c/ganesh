story_id: e03s03
spec: e03s03-trusted-capabilities-and-pi-boundary.md
title: Trusted Capabilities and Pi Boundary
status: passing
bcps: 7
tasks:
  - id: e03s03-t01
    description: Define non-serializable owner and worker capabilities with explicit protected operation scopes.
    verify: "npm run build && node --test --test-name-pattern='e03s03.*(capability|owner|worker|scope)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - authority
        - Security Review
  - id: e03s03-t02
    description: Protect canonical project writes, credentials, and project paths from forged roles, generic tools, and cross-project requests.
    verify: "npm run build && node --test --test-name-pattern='e03s03.*(forge|credential|canonical|project|write)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - isolation
        - Security Review
  - id: e03s03-t03
    description: Implement curated Pi resource loading and inert document rendering with built-in command and script or macro denial.
    verify: "npm run build && node --test --test-name-pattern='e03s03.*(curated|builtin|render|script|macro|injection)' dist/test/*.test.js"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - pi-boundary
        - adversarial
  - id: e03s03-t04
    description: Integrate ask, approve, and full-access execution modes with per-project bash guard outcomes and local-risk notices.
    verify: "npm run build && node --test --test-name-pattern='e03s03.*(ask|approve|full.access|bash|guard|mode)' dist/test/*.test.js"
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - local-execution
        - policy
