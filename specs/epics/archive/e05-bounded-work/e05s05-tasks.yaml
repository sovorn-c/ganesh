story_id: e05s05
spec: e05s05-provider-degradation-and-session-rebinding.md
title: Honest Provider Degradation and Session Rebinding
status: passing
bcps: 6
tasks:
  - id: e05s05-t01
    description: Record consented destination checks and deny mismatched or withdrawn provider calls without unauthorized fallback, with no new security findings in affected paths.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e05s05.*(destination|consent|withdraw|fallback)' dist/tests/*/*.test.js"
    risk: P0
    security: high
    scenarios: [SC-e05s05-P0-02]
    status: passing
    allure:
      severity: critical
      categories: [policy, providers, Security Review]
  - id: e05s05-t02
    description: Bound timeout and failure retries on the same consented destination, then fail the run with inspectable attempts.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e05s05.*(timeout|retry|failure)' dist/tests/*/*.test.js"
    risk: P0
    security: medium
    scenarios: [SC-e05s05-P0-01]
    status: passing
    allure:
      severity: critical
      categories: [providers, reliability]
  - id: e05s05-t03
    description: Rebind event subscriptions after session replacement and reject unbound prior-session submissions.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e05s05.*(rebind|replace|unbound|subscribe)' dist/tests/*/*.test.js"
    risk: P0
    security: high
    scenarios: [SC-e05s05-P0-03]
    status: passing
    allure:
      severity: critical
      categories: [sessions, Security Review]
  - id: e05s05-t04
    description: Keep uncertain usage reserved rather than zero, redact adapter errors, and prove released regressions pass with no new security findings in affected paths.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run typecheck && npm run lint && npm run build && npm test && node --test --test-name-pattern='e05s05.*(uncertain|zero|redact|regression)' dist/tests/*/*.test.js"
    risk: P1
    security: high
    scenarios: [SC-e05s05-P1-04]
    status: passing
    allure:
      severity: high
      categories: [honesty, privacy, regression, Security Review]
