story_id: e05s01
spec: e05s01-authorized-contract-and-specialist-run.md
title: Authorized Contract Admits One Least-Privilege Specialist Run
status: passing
bcps: 8
tasks:
  - id: e05s01-t01
    description: Add work-contract, run, snapshot and budget types plus exact v1 additive schema initialization, atomic reservation and retry/recovery coverage.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e05s01.*(schema|contract|reopen|migration|atomic|retry|recovery)' dist/tests/*/*.test.js"
    risk: P0
    security: medium
    scenarios: [SC-e05s01-P0-01, SC-e05s01-P0-03]
    status: passing
    allure:
      severity: critical
      categories: [work, persistence, provenance]
  - id: e05s01-t02
    description: Authorize exact contracts and standing permissions with owner-only capabilities, then dispatch one Supervisor run through current-policy checkpoints with no new security findings in affected paths.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e05s01.*(authorize|standing|dispatch|policy|withdraw)' dist/tests/*/*.test.js"
    risk: P0
    security: high
    scenarios: [SC-e05s01-P0-01, SC-e05s01-P0-02]
    status: passing
    allure:
      severity: critical
      categories: [authority, policy, Security Review]
  - id: e05s01-t03
    description: Reserve token/call/time before session start, accept a versioned candidate that is not a human commitment, and deny unassigned snapshot reads.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e05s01.*(reserv|candidate|commit|snapshot|assigned)' dist/tests/*/*.test.js"
    risk: P0
    security: high
    scenarios: [SC-e05s01-P0-01, SC-e05s01-P0-03, SC-e05s01-P1-04]
    status: passing
    allure:
      severity: critical
      categories: [work, authority, Security Review]
  - id: e05s01-t04
    description: Prove forged and worker authority failures, diagnostic redaction, no new security findings in affected paths, and all released E01-E04/E06 behavior remains passing.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run typecheck && npm run lint && npm run build && npm test && node --test --test-name-pattern='e05s01.*(capability|forg|redact|regression)' dist/tests/*/*.test.js"
    risk: P1
    security: high
    scenarios: [SC-e05s01-P0-02, SC-e05s01-P1-04]
    status: passing
    allure:
      severity: high
      categories: [privacy, recovery, regression, Security Review]
