story_id: e08s03
spec: e08s03-screening-eligibility-amendments-and-uncertainty-queue.md
title: Screening, Eligibility Amendments and Uncertainty Queue
status: passing
bcps: 5
tasks:
  - id: e08s03-t01
    description: Record include, exclude and uncertain screening decisions that survive reopen against the criterion version used.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e08s03.*(screening|include|exclude|uncertain|reopen)' dist/tests/*/*.test.js"
    risk: P0
    security: low
    scenarios: [SC-e08s03-P0-01]
    status: passing
    allure:
      severity: critical
      categories: [literature, screening, provenance]
  - id: e08s03-t02
    description: Amend eligibility by creating a new protocol version without rewriting historical screening rows or their criterion versions and reasons.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e08s03.*(amendment|protocol version|historical|criterion)' dist/tests/*/*.test.js"
    risk: P0
    security: low
    scenarios: [SC-e08s03-P0-02]
    status: passing
    allure:
      severity: critical
      categories: [literature, eligibility, history]
  - id: e08s03-t03
    description: Reject universal saturation and paper-count stop rules with unjustified-threshold while still exposing descriptive included/excluded/uncertain counts.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e08s03.*(unjustified-threshold|saturation|paper-count|descriptive)' dist/tests/*/*.test.js"
    risk: P0
    security: low
    scenarios: [SC-e08s03-P0-03]
    status: passing
    allure:
      severity: critical
      categories: [literature, research-integrity, D-12]
  - id: e08s03-t04
    description: Keep the uncertainty queue inspectable, isolate unrelated exclusions when one entry is resolved, and keep released E01-E07/E14 behavior passing.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run typecheck && npm run lint && npm run build && npm test && node --test --test-name-pattern='e08s03.*(uncertainty|queue|unrelated|regression)' dist/tests/*/*.test.js"
    risk: P1
    security: medium
    scenarios: [SC-e08s03-P1-04]
    status: passing
    allure:
      severity: high
      categories: [literature, uncertainty-queue, regression]
