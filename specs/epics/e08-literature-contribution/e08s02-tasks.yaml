story_id: e08s02
spec: e08s02-authorized-retrieval-citation-exploration-and-coverage.md
title: Authorized Retrieval, Citation Exploration and Honest Coverage
status: passing
bcps: 6
tasks:
  - id: e08s02-t01
    description: Add an injected retrieval adapter, search-event and corpus-snapshot records that retain provider caps, failed pages, inaccessible sources and revised queries with criterion or query version and reason.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e08s02.*(provider cap|failed page|inaccessible|revised query|search event)' dist/tests/*/*.test.js"
    risk: P0
    security: low
    scenarios: [SC-e08s02-P0-01]
    status: passing
    allure:
      severity: critical
      categories: [literature, retrieval, AC-16]
  - id: e08s02-t02
    description: Replay saved corpus snapshots without calling the adapter, and record live reruns as new search events that never overwrite prior snapshots.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e08s02.*(snapshot|live rerun|replay)' dist/tests/*/*.test.js"
    risk: P0
    security: low
    scenarios: [SC-e08s02-P0-02]
    status: passing
    allure:
      severity: critical
      categories: [literature, provenance, AC-16]
  - id: e08s02-t03
    description: Add retrieval to disclosure operation kinds, deny local-only query artifacts to remote destinations, record paywalled results as coverage limits without a second-destination bypass, and prove no new security findings in affected disclosure paths.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run build && node --test --test-name-pattern='e08s02.*(local-only|remote|paywall|inaccessible|disclosure)' dist/tests/*/*.test.js"
    risk: P0
    security: high
    scenarios: [SC-e08s02-P0-03]
    status: passing
    allure:
      severity: critical
      categories: [disclosure, privacy, Security Review]
  - id: e08s02-t04
    description: Explore citations from E06 bibliographic records as bounded relational edges without sockets, and keep released E01-E07/E14 behavior passing.
    verify: "node -e \"if (process.versions.node.split('.')[0] !== '24') process.exit(1)\" && npm run typecheck && npm run lint && npm run build && npm test && node --test --test-name-pattern='e08s02.*(citation|bibliographic|regression)' dist/tests/*/*.test.js"
    risk: P1
    security: medium
    scenarios: [SC-e08s02-P1-04]
    status: passing
    allure:
      severity: high
      categories: [citation-exploration, research-integrity, regression]
